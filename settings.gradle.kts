rootProject.name = "petus-launcher"

// The Fabric client mod (petus-connect) and the server plugin (petus-auth) are
// standalone Gradle builds (they need fabric-loom / paperweight with their own
// plugin repositories), so they are intentionally NOT included here.
